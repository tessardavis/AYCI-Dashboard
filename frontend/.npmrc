# react-day-picker pins date-fns ^2 || ^3 but the rest of the app uses 4.x.
# legacy-peer-deps tells npm to install anyway (the runtime behaviour is fine).
# Without this, fresh `npm install` and Vercel/Render builds fail with ERESOLVE.
legacy-peer-deps=true
